len(nums)*len(nums[0]) col:=lecoln(nums[0])
Travel from 0 to len.
rows i /len(col) columns i%len(col)
Target array number of res rows i /c columns i %c
The time complexity is O(n), and the space complexity is O(1).
The memory consumption is 6Mb.
